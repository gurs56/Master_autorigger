import maya.cmds as cmds

def main():
    
    jointLeg = []
    for i in process.get_option("Spine", group="Joint"):
        if "JNT_thigh" in i:
            joints.append(i)
        else if "JNT_calf" in i
    
    return