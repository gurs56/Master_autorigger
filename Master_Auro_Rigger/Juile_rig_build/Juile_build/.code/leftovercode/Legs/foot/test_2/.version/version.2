
def main():
    
    for side in ['L','R']:
        #This is needed because the foot roll exists on same joint as the ik leg
        cmds.delete('buffer_foot_%s_orientConstraint1' % side.upper())
        cmds.orientConstraint('locator_foot_%s' % side.upper(), 'buffer_foot_%s' % side.upper())
    
    return