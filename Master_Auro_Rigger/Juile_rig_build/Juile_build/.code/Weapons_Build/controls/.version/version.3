
def main():
     
    #Spawn joint to give control something to attach too
    joints = cmds.joint()
    BladeJnt = process.get_option("Root", group = "Joint")  # Only pass the option name
    
    rig = rigs.
    
    return