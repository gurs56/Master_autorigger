
def main():
    
    # vars
    rigGrp = process.get_option( 'rig Grp' , group = 'Groups' )
    controlsGrp = process.get_option( 'controls Grp' , group = 'Groups' )    
    setupGrp = process.get_option( 'setup Grp' , group = 'Groups' )    
    
    # grp of creating ground, gear, cog controls
    
    # create controls and setup top grp
    
    cmds.group( em = 1 , n = controlsGrp , p = rigGrp )    
    cmds.group( em = 1 , n = setupGrp , p = rigGrp )    
    
    
    return