from vtool.maya_lib import rigs
from vtool.maya_lib import rigs_util

def main():
    
    # vars
    controlsGrp = process.get_option( 'controls Grp' , group = 'Groups' )
    controlsAttr = 'controlsVis'
    structureGrp = process.get_option( 'structure Grp' , group = 'Groups' )
    structureAttr = 'structureVis'
    modelGrp = process.get_option( 'model Grp' , group = 'Groups' )
    ModelAttr = 'modelVis'
    sizex, sizey, sizez =  cmds.getAttr('%s.boundingBoxMax'%modelGrp)[:2]
    
    # gear cnt
    gear = rigs_util.Control('Gear')
    gear.create_xform(prefix='xform')
    gear.translate_shape(0, sizey*1.2, 0)
    gear.rotate_shape(0, 0, 90)
    gear.scale_shape(sizex*0.3, sizex*0.3, sizex*0.3 ,use_pivot=False)
    gear.hide_scale_and_visibility_attributes()
    gear.hide_translate_attributes()
    gear.hide_rotate_attributes()
    #gear.hide_attributes(attributes='ro')
    
    # create attrs on gear cnt
    print gear
    
    
    return