
def main():
    
    # vars
    sides = ['l', 'r']
    source_list = [ 'CNT_SUB_GROUND_2', 'CNT_SUB_COG_C', 'CNT_SUB_HIPS_C' ]
    
    # space switches
    
    for side in sides:
        
        cnt = 'CNT_IK_LEG_BTM_1_%s' % side.upper()
        ikDriver = cmds.listRelatives(cnt, p=1)[0]
        
        # space switch setup on cnt drivers
        rig = space.create_multi_follow(
                source_list,
                ikDriver,
                node=cnt,
                constraint_type='parentConstraint',
                attribute_name='follow',
                value=0,
                create_title=True
                )
        
    
    return