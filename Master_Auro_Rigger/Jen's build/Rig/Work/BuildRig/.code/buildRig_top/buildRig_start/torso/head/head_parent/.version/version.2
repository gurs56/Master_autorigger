from vtool.maya_lib import space

def main():
    
    # vars
    sides = ['l', 'r']
    source_list = [ 'CNT_NECK_2_C', 'CNT_SUB_COG_C', 'CNT_GROUND_1' ]
    
    # space orient switch
    
    cnt = 'CNT_HEAD_1_C'
    ikDriver = cmds.listRelatives(cnt, p=1)[0]
    
    # space switch setup on cnt drivers
    rig = space.create_multi_follow(
            source_list,
            ikDriver,
            node=cnt,
            constraint_type='orientConstraint',
            attribute_name='followRotate',
            value=0,
            create_title=True
            )
        
    
    return